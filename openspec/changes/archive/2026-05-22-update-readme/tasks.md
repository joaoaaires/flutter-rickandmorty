## 1. Update English README

- [x] 1.1 Update Flutter SDK version in Prerequisites from `(>=2.15.1)` to `(>=3.12.0)` in `README.md`

## 2. Update Portuguese README

- [x] 2.1 Update Flutter SDK version in Pré-requisitos from `(>=2.15.1)` to `(>=3.12.0)` in `README.pt-br.md`

## 3. Verify Consistency

- [x] 3.1 Confirm both READMEs have identical SDK version and dependency information
