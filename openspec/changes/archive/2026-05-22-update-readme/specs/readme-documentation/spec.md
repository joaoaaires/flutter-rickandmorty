## MODIFIED Requirements

### Requirement: Prerequisites section accuracy
The Prerequisites section SHALL state the minimum Flutter SDK version required to build and run the project.

#### Scenario: Updated SDK version displayed
- **WHEN** a user reads the Prerequisites section in README.md
- **THEN** the Flutter SDK version SHALL read `(>=3.12.0)` instead of `(>=2.15.1)`
