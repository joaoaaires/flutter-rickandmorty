## MODIFIED Requirements

### Requirement: Pré-requisitos section accuracy
The Pré-requisitos section SHALL state the minimum Flutter SDK version required to build and run the project.

#### Scenario: Updated SDK version displayed in Portuguese README
- **WHEN** a user reads the Pré-requisitos section in README.pt-br.md
- **THEN** the Flutter SDK version SHALL read `(>=3.12.0)` instead of `(>=2.15.1)`
